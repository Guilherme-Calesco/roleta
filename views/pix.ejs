<% const pageTitle = 'Pagamento PIX' %>
<%- include('partials/header') %>

<section class="card center pix-page">
  <h2 class="display">Faça o PIX 💛</h2>

  <div class="pix-amount">R$ <%= Number(amount).toFixed(2).replace('.', ',') %></div>

  <div class="qr-wrap">
    <img src="<%= qrDataUrl %>" alt="QR Code PIX" />
  </div>

  <div class="pix-info">
    <div class="pix-row">
      <span class="pix-label">Recebedor</span>
      <span class="pix-value"><%= receiverName %></span>
    </div>
    <div class="pix-row">
      <span class="pix-label">Copia e Cola</span>
      <button type="button" class="button-link" id="copy-pix" data-pix="<%= pixPayload %>">Copiar</button>
    </div>
  </div>

  <div class="countdown-block">
    <p class="hint" id="countdown-text">Aguarde <span id="countdown">30</span>s...</p>
    <button
      type="button"
      class="button-premium"
      id="confirm-btn"
      disabled
    >CONFIRMAR PIX</button>
  </div>
</section>

<script src="/js/pix.js"></script>
<%- include('partials/footer') %>
